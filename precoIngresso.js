const readline = require('readline-sync');

let nome = "kauan";
let idade = 16;
let temCupon= true;
let precoFinal= " ";

const PRECO_CHEIO = 30;

if (idade >= 60, precoFinal = PRECO_CHEIO / 2);

else if (idade < 18, temCupon = true, precoFinal = PRECO_CHEIO * 0.8);

else (precoFinal === PRECO_CHEIO);

console.log(`Olá, ${nome}`);
console.log(`o valor do seu ingresso é ${precoFinal}`);